# Reto IA 3 - CircuitBreaker

Entregable completado en `../circuit_breaker.py`.

Metodos completos: `estado`, `_revisar_timeout`, `_es_fallo_servidor`, `_registrar_exito`, `_registrar_fallo`, `_abrir`, `ejecutar`.

La version final incluye decisiones de diseno y tabla de clasificacion en el docstring inicial.
